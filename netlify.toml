[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA: cualquier ruta devuelve index.html (seguro para refrescar URLs).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200